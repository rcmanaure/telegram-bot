# ─── API Keys ───────────────────────────────────────────────
OPENROUTER_API_KEY=sk-or-...        # https://openrouter.ai (used for LLM + embeddings)
TELEGRAM_BOT_TOKEN=...              # From @BotFather
GROQ_API_KEY=                       # STT for voice notes — get from console.groq.com

# ─── Database ───────────────────────────────────────────────
# Use @postgres when running via docker-compose; @localhost for local dev
DATABASE_URL=postgresql+asyncpg://ragbot:ragbot@postgres:5432/ragbot
DATABASE_URL_SYNC=postgresql://ragbot:ragbot@postgres:5432/ragbot

# ─── Models ─────────────────────────────────────────────────
LLM_MODEL=openrouter/owl-alpha                  # default; swap for anthropic/claude-haiku-4.5 etc.
LLM_FALLBACK_MODEL=openrouter/free              # fallback when primary fails; empty = disabled
EMBEDDING_MODEL=openai/text-embedding-3-small   # via OpenRouter
EMBEDDING_DIM=1536

# ─── RAG Settings ───────────────────────────────────────────
CHUNK_SIZE=500          # characters per chunk
CHUNK_OVERLAP=50        # overlap between chunks
TOP_K_RESULTS=4         # how many chunks to retrieve

# ─── App ────────────────────────────────────────────────────
APP_HOST=0.0.0.0
APP_PORT=8000
APP_DOMAIN=localhost:8000  # prod: set to real domain (e.g. srv1546906.hstgr.cloud)
TRAEFIK_HOST=              # prod: same domain — used by docker-compose Traefik labels
DEFAULT_NAMESPACE=demo  # default document namespace
